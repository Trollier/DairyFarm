@{ Layout = null;}
@model DairyFarm.Core.DAL.Cattle

<h4>Etes Vous sur de vouloir supprimer la bête au code :</h4>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.CodeCattle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CodeCattle)
        </dd>
    </dl>

    @using (Html.BeginForm("Delete","Cattle")) {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model=>model.IdCattle)
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> 
        </div>
    }
</div>
